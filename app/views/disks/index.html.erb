<%= @item.item_name %> <br>
ディスクの追加
<%= form_for [@item, @disk], url: item_disks_path do |f| %>

ディスクNo.<br>
<%= f.number_field :disk_number %><br>

<%= f.submit 'ディスクを追加する' %>

<% end %><br>

<% @disks.each do |disk| %>
	<%= disk.id %>
	<%= link_to "#{disk.disk_number}", item_disk_songs_path(@item, disk) %>
	<%= link_to "削除", item_disk_path(@item, disk), method: :delete %><br>

<% end %>
