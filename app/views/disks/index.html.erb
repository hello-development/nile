<%= @item.item_name %> <br>
ディスクの追加
<%= form_for [@item, @disk], url: item_disks_path do |f| %>

ディスクNo.<br>
<%= f.number_field :disk_number %><br>

<%= f.submit 'ディスクを追加する' %>

<% end %><br>

<% @disks.each do |disk| %>
	<%= disk.id %>
	<%= disk.disk_number %><br>

<% end %>