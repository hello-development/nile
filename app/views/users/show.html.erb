<!DOCTYPE html>
<html>
  <head>
    <title>NILE</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <div class="message"> <%= flash[:notice] %> </div>

    <div class="users_show_upper clearfix" >

    <div class="users_show_left">
      <h4 class="users_names"> <strong> ユーザー名 </strong> </h4>
      <div> <%= current_user.last_name %> <%= current_user.first_name %> さん </div>
      <div> <%= link_to "編集", edit_user_path, class: 'btn btn-default' %> </div>
      <div> <%= link_to "退会", root_path, class: 'btn btn-default', method: :'delete', "data-confirm" => "本当に退会しますか？" %> </div>
    </div>

    <div class="users_show_right">
    <h4 class="favorite_artists"> <strong> お気に入りアーティスト </strong> </h4>
    <div> <%= @artist.artist_name %> </div>
    <div> <%= @artist.artist_image %> </div>
    <div> <strong> 最新のCD </strong> </div>
    <div> <%= @artist.artist_image %> </div>
    <div> <%= @item.item_name %> </div>
    <div> <%= @genre.genre_name %> </div>
    <div> ￥ <%= @item.price %> </div>

    </div>

    <div class="mypage_upperright_right">
    <h4 class="favorite_ranking"> <strong> いいねランキング </strong> </h4>
    <table>
      <%# @artists.each do |artist| %>
      <tbody>
        <tr>
          <div> <%= @artist.artist_image %> </div>
          <div> <%= @item.item_name %> </div>
          <div> カートに入れる </div>
        </tr>
        <tr>
          <div> <%= @artist.artist_image %> </div>
          <div> <%= @item.item_name %> </div>
          <div> カートに入れる </div>
        </tr>
        <tr>
          <div> <%= @artist.artist_image %> </div>
          <div> <%= @item.item_name %> </div>
          <div> カートに入れる </div>
        </tr>
      </tbody>

    </table>
    </div>

    </div>

<div class="users_show_bottom">
<div class="purchase_histories"> 購入履歴 </div>
<div>　月　日購入▼</div>
</div>

  </body>

  </html>